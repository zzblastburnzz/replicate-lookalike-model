build:
  gpu: true
  python_version: "3.10"
  python_packages:
    - torch
    - torchvision
    - diffusers
    - transformers
    - accelerate
    - safetensors
    - pillow

predict: "predict.py:Predictor"
